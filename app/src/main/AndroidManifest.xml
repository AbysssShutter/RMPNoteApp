<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launchericon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launchericon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RMPNoteApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.joke.JokesOnYou"
            android:exported="false" />
        <activity
            android:name=".ui.MapWithPoint"
            android:exported="false" />
        <activity
            android:name=".ui.ChangeNote"
            android:exported="false" />
        <activity
            android:name=".ui.AddNewNoteScreen"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RMPNoteApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>